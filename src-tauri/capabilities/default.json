{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window, including reviewed app commands db_execute and db_transaction",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "allow-db-execute",
    "allow-db-transaction"
  ]
}
